// Compatibility export for fleet callers. The hardened implementation lives in
// src/lib/safe-compare.ts as the shared single source of truth.
export { safeStringEqual } from '../lib/safe-compare.ts';
