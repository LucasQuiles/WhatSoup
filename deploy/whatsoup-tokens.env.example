# WhatSoup instance health token — used to authenticate POST /send, /access, /heal endpoints.
# Generate with: openssl rand -hex 32
# Place at: ~/.config/whatsoup/instances/<instance-name>/tokens.env
# Permissions: chmod 600
WHATSOUP_HEALTH_TOKEN=replace-with-generated-token
