# Semantic module graph fixtures

The module-graph tests construct these source shapes in memory so each edge and export-ownership
case remains visible beside its assertion. This directory reserves the fixture boundary for larger
multi-file graphs that cannot be expressed clearly inline.
