# draft(cluster): observability gaps — freshness, drift, classification (7 P1-P2 issues)

> **Triage catalog — not a closure record, and not a PR body.**
> Authored as `draft(cluster)` PR bodies (#2763–#2781; all closed, **none merged**) and
> committed verbatim by #2787 for their grouping and per-issue summaries. `Tracks #N` is a
> reference, never a closing directive — the original `Closes` keyword bound only to the
> issue immediately after it and asserted closure of work that is largely still open. The
> original `All guards pass` line has been removed: no per-issue verification stands behind
> it. Read the issues for real status.

Tracks #2340, #2385, #2462, #2572, #2564, #2408, #2432

Content-free execution-holder age/progress freshness (#2340, P1). Correlate persistent multi-asset release drift into one member-aware alert (#2385, P1). Daily health needs durable receipt separating probe execution from health verdict (#2462, P1). Startup readiness and context failures invisible to health (#2572, P1). Raw forensic context persisted and retransmitted as repair evidence — heal boundary (#2564, P1 PATCH READY). Distinguish MCP probe failures from missing tools and bind inventory expiry (#2408, P2). Fleet sentinel watchdog ignores fresh non-green aggregate health (#2432, P2).
