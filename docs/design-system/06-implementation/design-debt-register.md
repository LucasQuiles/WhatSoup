# Design Debt Register — SOUP v3 Implementation (SSOT)

Schema per qa-hardening §8. Status: open unless marked. Closed items move to the bottom table.
Lint waivers (WVR-*) live in `console/eslint-waivers.yaml` and are not duplicated here.

| ID | Title | Area | Type | Sev | Reason | Workaround | Owner / next step | Cleanup phase | Expiration condition | Blocks final acceptance? |
|---|---|---|---|---|---|---|---|---|---|---|
| DD-5 | Theme toggle minimal ghost button | component | component | P3 | final nav treatment belongs with nameplate work | functional toggle shipped | C3 nav slice | C3 | nameplate slice merged | no |
| DD-9 | Legacy half-step spacing aliases (--sp-0h/1h/2h, 14px/7px) | layout | token | P3 | consumed by composites pending migration | tokens-v3 disposition mapped | per-directory C2/C3 | C3 | composites absorbed into primitives | no |
| DD-18r | Responsive hardening backlog — NARROWED 2026-06-12: the deterministic-viewport-tests leg DELIVERED for Fleet/LineDetail/Ops (`tests/browser/viewport-matrix.test.tsx` `d80e9726` — lg stacking flip probed at 1023/1024, ≥40-char h1 truncation + 9-tab x-scroll at 390, overflow sweeps at five widths + 1440×500 — in CI `8c26fbb1`, reproduced fresh in d7-evidence.md); B4 landed the Inbox contact-pane collapse (`68a7beda`). RE-NARROWED 2026-06-12 (b4-evidence.md staleness flag, integrator-verified): Inbox viewport-matrix rows + the deterministic drawer-flip case DELIVERED at `4fbad4ef` (Inbox rows, 900px drawer position=static / 899px scrim proofs); MessageBubble positioning CLOSED at B4 `d5627923` (b4-evidence.md, with a geometry-glance residue noted for the C3 visual round). Legacy modal sizing SSOT leg STRUCK 2026-06-12 (b3-wave4-evidence.md verified all four consumed panel tokens deleted; `--panel-shortcuts` zero-consumer orphan named as a ride-along deletion candidate for C3). REMAINING legs: nav width pressure beyond label hiding, side-panel law for non-Fleet surfaces | responsive | layout | P1 | surfaces predate layout-density spec | desktop-first works; delivered matrix legs now regress in CI instead of waiting for live QA | each named surface slice + the D7 follow-up suites (Inbox rows, drawer flip) | C3/D7 | every named item PASS or NOT-APPLICABLE in the QA matrix | YES |
| DD-22 | LogStream renders bounded poll snapshots; no virtualization/live-tail streaming | component | component | P3 | log-stream.md requires virtualization for unbounded data; today getLogs returns bounded snapshots polled at 3s — no streaming source exists | bounded render + maxEntries tail cap (drawer last-50) | LogStream maturity slice; product decision required for SSE/WS log streaming | future | before any live-tail/streaming log feature ships | no |
| DD-23 | Popover viewport-edge handling — bottom-only placement, no collision flip | component | responsive | P3 | proposed by the picker-family investigation; no popover sits in a bottom-fold surface today | bottom placement only | first bottom-fold popover consumer or deterministic viewport tests | D7 | collision flip proven or NOT-APPLICABLE ruling in the QA matrix | no |
| DD-24 | Contact-pane actions (Mark Read / Allow / Block / Save Contact) unreachable below the Inbox collapse threshold | interaction | responsive | P2 | spec-mandated pane drop removes the only path to four actions; inventing an unspecified surface was out of scope for the collapse commit | actions absent at narrow widths | alternate action path on the thread surface designed against button/toolbar specs in a later slice | C3 | alternate path lands or explicit NOT-APPLICABLE ruling in the QA matrix | no |
| DD-26 | Type-ramp bridge is defined but not spec-closed — 8 currently consumed `--type-*` names now exist in `tokens.primitive.css` as exact fallback-preserving bridge values, while the full tokens-v3 ramp is 12 tokens and the bridge values intentionally differ from the spec ramp | typography | token | P2 | first surfaced as zero CSS definitions behind 24 fallback-carrying consumers; closeout bridge removed the live undefined-token path without changing rendering, but did not implement the final type scale | consumers still carry explicit fallbacks and the bridge values mirror those fallbacks; `tests/console/design-token-type-ramp.test.ts` pins the bridge and the 4 pending spec tokens | implement the full 12-token ramp (`display`, `title`, `heading`, `body`, `body-st`, `label`, `caption`, `overline`, `data-lg`, `data`, `data-sm`, `nameplate`) per tokens-v3 with a visual checkpoint, then remove redundant fallbacks | C3 | all 12 ramp tokens are defined to spec values, fallbacks become redundant, dangling-ref check stays at zero, and the bridge test is retired/replaced by spec-drift enforcement | no |
| DD-28 | Stale-data state has no spec home — qa-hardening treats stale as a first-class QA case but no component spec defines its rendering | state-model | spec | P3 | found by the state-taxonomy index; surfaces poll-refresh data with no staleness affordance defined | undefined; per-surface ad hoc | define in the relevant component specs or record a not-applicable ruling during the C3 screen passes | C3 | every slice QA matrix can cite a spec home or an explicit ruling for stale | no |
| DD-32 | C-B3W2-4 seg naming/extraction ruling never registered — the wave-2 segmented-control pattern shipped inline in two dialogs; whether it extracts to a shared composite (or the inline form is canonical) was deferred at wave 3 and never recorded | component | spec | P3 | b3-wave2-evidence.md omission audit | six pairs work on the inline pattern | record the ruling (extract vs canonical-inline) during the C3 consolidated pass — ✓ RESOLVED 2026-06-17 (#1035): ruling = EXTRACT. A generic `Segmented` primitive owns the `.soup-toolbar-seg` recipe; `ToolbarTimeRange` is a thin alias and the DateTimePicker recurrence row reuses it (duplication retired) | C3 | a documented ruling exists | no |
| DD-33 | Per-dialog focus-restore pins missing — wave-2's planned "focus restores to opener" tests per dialog were never written; compensated at primitive level (primitives-modal.test.tsx restore pins) + wave-1 sibling suites | test | test | P3 | b3-wave2-evidence.md omission audit | primitive-level restore proof covers the shared path | add per-dialog restore pins opportunistically in C3 screen passes or rule primitive-level proof sufficient | C3 | pins added or sufficiency ruling recorded | no |
| DD-34 | UpdateModal has no live-QA matrix row — every other migrated dialog has live visual confirmation; UpdateModal's shell is the same Modal at size=sm and behavior carries 39 jsdom pins, but no frame was captured | qa | qa | P3 | b3-wave3-evidence.md C-B3W3-3 residue | shared-shell confirmation via matrix rows 3/5/6/8 + 39-test suite | capture one UpdateModal frame in the next C3 visual round | C3 | matrix row added or NOT-APPLICABLE ruling | no |
| DD-36 | Icon size ramp unenforced — raw ad-hoc `size={N}` off the iconography.md §2 ramp (12/16/18/20/24). 7 distinct icon-scale (≤24px) off-ramp values in use — 13/14/15 near-misses plus 6/9/10/11 which the spec says "do not exist" (below the 12px floor, tokens-v3 §8); ~80 icon call-sites carry hardcoded sizes; the `icon-size-ramp` lint is named at iconography.md:60 but never built | iconography | token+lint | P2 | hardcoded ad-hoc icon sizes predate any ramp enforcement; sub-12 sizes violate the legibility floor outright | frozen-inventory ratchet pins the current off-ramp set and fails on any NEW off-ramp size (`tests/console/icon-size-ramp-inventory.test.ts`), so drift cannot spread during migration | migrate off-ramp sizes (13/14/15 → 16; sub-12 6/9/10/11 → 12 info-only) then promote to the eslint `icon-size-ramp` error per the lint-plan lifecycle; Systems-coordinated | C3 | off-ramp debt allowlist empties and `icon-size-ramp` promotes to scoped/global error | no |
| DD-37 | Typography 12px legibility floor unenforced + ramp self-violates — typography.md:24 mandates "Minimum data text is 12px; nothing renders smaller", and :76 itself flags `--text-xs` (9.6px) as below-floor; legacy `--text-label` (10.4px)/`--text-sm` (11.2px) are consumed, and the DD-26 fallback bridge also pins sub-floor `--type-caption` 11px, `--type-data-sm` 11px, `--type-label` 11px, and `--type-overline` 10px until the final visual type-ramp pass | typography | token+lint | P2 | the spec floor predates enforcement and the fallback bridge deliberately preserves today's below-floor rendering to avoid an unreviewed visual jump | frozen-inventory ratchet pins the 3 legacy sub-floor tokens and fails on any NEW one (`tests/console/typography-floor-inventory.test.ts`, 0dd2f29e); `tests/console/design-token-type-ramp.test.ts` pins the bridge values separately | raise/migrate sub-floor consumers onto `--text-data`/a sanctioned caption tier, then update the DD-26 bridge from fallback values to the final 12-token type ramp in a visual-checkpointed pass; Systems-coordinated | C3 | the sub-floor token set empties (or the spec sanctions a caption tier) and the floor rule promotes | no |
| DD-39 | 46 `color-mix()` uses carry zero `@supports` fallback — status washes / chrome-glass / channel tints vanish entirely on engines without color-mix (pre-2023 Safari/WebView). CORRECTED 2026-06-15: chrome-glass (tokens.semantic.css:100/200) NO LONGER holds a hex fallback — it was dereffed to `color-mix(in srgb, var(--surface-base) 86%, transparent)`, so ZERO of the 46 surfaces (incl. the critical chrome) carry a fallback now | color | durability | P3 | durability gap on old engines; no spec guidance on the fallback pattern; the one surface that used to degrade gracefully (chrome-glass) no longer does | modern browsers render correctly; on pre-color-mix engines every color-mix surface is unfallback'd | document an `@supports (color-mix(...))` fallback pattern in tokens-v3 + add a guard asserting fallback presence (or a documented min-engine ruling); Systems-coordinated | C3/future | every color-mix surface has a fallback or a recorded min-engine ruling | no |
| DD-44 | RESOLVED 2026-06-15 → commit `46977ac9`: `hsl(H,45%,34%)` fills + a fixed `--avatar-ink` (#fff) clear WCAG AA 4.5:1 for all 8 hues × both themes (min 4.76); the same commit reconciled the orphaned `tokens.primitive.css` deletion that had silently broken group avatars. Original — Avatar hue-fill initials fail WCAG AA — the `var(--avatar-hue-N)` solid fill (`hsl(H,45%,55%)`) + `--t1` ink (groups-utils.ts/GroupCard/GroupDetailModal) computes below 4.5:1 small-text: **dark theme all 8 hues fail** (1.78–4.06:1; warm/green/cyan H45–180 ≈1.8), **light theme 4/8 fail** (red/blue/purple/magenta 3.46–4.19). Surfaced + computed (8 hues × 2 themes) while modeling the Avatar specimen faithfully | color | accessibility | P2 | mid-tone hues at L55% don't contrast with near-white ink in dark theme nor with near-black ink for the cool hues in light | initials remain partly legible and the full name always travels in an adjacent label in product | DECIDED (8×2 matrix computed 2026-06-15): root cause is the theme-dependent `--t1` ink (white dark / black light) on a fixed mid-tone fill — L55 fails one theme (worst 1.78). Fix = darken `--avatar-hue-0..7` to `hsl(H,45%,34%)` AND switch the avatar-initial ink from `--t1` to a FIXED white token → clears 4.5:1 for all 8 hues × 2 themes (min 4.76); land in tokens.primitive.css + GroupCard/GroupDetailModal/groups-utils, explicit-path, no push | C3 | every hue × theme avatar-initial pair ≥ 4.5:1 | no |

| DD-48 | Net-new toolkit primitives — Slider / Progress / Pagination / Command-palette + §17 forward-kit (inline-edit, bulk-action-bar) | component | component | P3 | DD-43 closeout residual: these were never cutover gaps (the row's own text: "net-new"); no consumer surface exists for them today, so they are future-program builds, not debt from the SOUP v3 cutover | inline/native equivalents cover current surfaces (e.g. raw `datetime-local` retired with DateTimePicker; no slider/progress use-case on the tree) | build each primitive (with focus/motion laws baked in) when its first consumer surface is named; command-palette is the highest-value of the set for an operator console | future | each primitive lands (or a NOT-APPLICABLE ruling is recorded) alongside its first consumer | no |

## Closed

| ID | Title | Closed by | Evidence |

| DD-38 | Card primitive — raw `.c-card` recipe consumers | CLOSEOUT 2026-07-19: the recipe was ABSORBED into the Card primitive — Card.tsx renders `.soup-card` (primitives.css, declarations verbatim + the `[tabindex]:focus-visible` rule moved with it); the legacy `.c-card` composite recipe is deleted; the `no-card-recipe-inventory` guard's allowlist is EMPTY (promoted state: any raw usage hard-fails); 7 test files' class pins co-updated | `console/src/styles/primitives.css` (`.soup-card` band), `console/src/styles/composites.css` (recipe deleted), `console/src/components/primitives/Card.tsx`, `tests/console/no-card-recipe-inventory.test.ts` (empty allowlist); full battery 173/174 (doc-drift environmental-timeout only, 19/19 solo), DS 66/66, design-regression.sh rc=0 |
| DD-35 | Accent-law residue — legacy split wordmark spending green on "Soup" | VERIFIED-CLOSED 2026-07-19: the remaining leg was falsified fresh — zero `>What<`/`>Soup<` split siblings in console/src, zero green-on-Soup spans, Nav renders the `.soup-nameplate` lockup (tick + SOUP wordmark per brand.md §1), design-regression.sh rc=0 with check 5/6/7 all at 0 matches (C4 straggler PR also landed the index.html title flip + guide vocabulary) | `console/src/components/Nav.tsx:59-65` (soup-nameplate), design-regression.sh check 6 (split-wordmark evasion) = 0 matches fresh run, PR #1936 (title + vocabulary flips) |
|---|---|---|---|
| DD-8 | text-3/ghost-tier essential-use audit | CLOSED 2026-07-19: remaining leg executed — fresh whole-tree sweep (31 ghost consumers) classified per the decision package's own rule (pointer-gated redundancy ≠ guard-satisfying). PROMOTED to text-2 (6): `.soup-log__lvl--debug` ink (Option B optional leg; dashed text-3 BORDER keeps debug distinct), Inbox:325 load-more, ActiveHoursHeatmap axis labels ×5, UnlockScreen:39 instructional copy, FleetTokenChart:124 TOKENS caption, `.soup-menu__label` (the bare c-col-header class). RULINGS recorded (STAYS): Landing:173 decorative, providers.ts:108 non-text, SoupKitchen:1260 zero-count, FeedCard:464 supplementary provenance, Operator:364-366 footer (toolbar-redundant), + the package's exempt families re-confirmed on the current tree. Spec amended log-stream.md:27 (D = --text-2 + dashed border). Size floor (9px letter) stays DD-37's domain | `oc-re/audits/2026-07-19-dd8-ghost-tier-remaining-map.md` (classification table); primitives.css:1125/1158/1811; suites 113/113 core + 12/12 heatmap + DS 66/66 + drift 33/33, design-regression.sh rc=0 |
| DD-43 | Toolkit gaps — reusable primitives (Accordion/Menu/Avatar/Card/Stepper/DateTimePicker/Segmented/Bottom-sheet/Switch) | CLOSED 2026-07-19: the expiration condition (P0 set built + barrel-exported) was MET 2026-06-17; P1 verified complete fresh — **Bottom-sheet is landed and consumed** (the row's "remaining" claim was stale): `DrawerPlacement='bottom-sheet'` with modal semantics + portal + full-screen scrim (`primitives/Drawer.tsx:136`), complete CSS band (`primitives.css:1360-1426` incl. closing + reduced-motion), the `Drawer — bottom-sheet placement (modal)` contract suite green (36/36 with Switch), and a live consumer (`SoupKitchen.tsx:168` narrow-viewport inspector); Switch barrel-exported (`primitives/index.ts:151`). Residual P2 items (Slider/Progress/Pagination/Command-palette) + §17 forward-kit re-registered as DD-48 — net-new, not cutover gaps (the row's own classification) | `console/src/components/primitives/Drawer.tsx:25-50,136`; `console/src/styles/primitives.css:1360-1426`; `tests/console/primitives-drawer.test.tsx` (bottom-sheet modal suite); `console/src/pages/SoupKitchen.tsx:168`; register DD-48 |
| DD-29 | Console-transport-loss (disconnected) state has no spec home | RESOLVED: ruled (decision-log DD-29) to need a dedicated connection-status surface, now built — app-level `ConnectionBanner` (WARN, above `<main>`, shown only while disconnected), `EmptyState variant="offline"` on failed primary loads while disconnected (SoupKitchen + Inbox; Operator/LineDetail are a tracked follow-up), and a one-shot "Connection restored" recovery toast; derived by `useTransportStatus` from `useRealtime().connected` + `navigator.onLine`. state-taxonomy finding #3 marked resolved and the state added to its §1.2 table | `console/src/hooks/use-transport-status.ts`; `console/src/components/ConnectionBanner.tsx`; `console/src/components/EmptyState.tsx`; `console/src/App.tsx`; `docs/design-system/02-directions/decision-log.md` (DD-29); `docs/design-system/03-spec/state-taxonomy.md` §1.2/§3; `tests/console/use-transport-status.test.ts`, `tests/console/connection-banner.test.tsx`, `tests/console/empty-state.test.tsx` |
| DD-10 | 24px target floor | Sm interactive pills now use a real 24px computed target instead of the 20px + bottom-pseudo workaround Chromium would not hit-test; removable-X keeps its 16px glyph but has top and bottom `::before` reach proven in real Chromium; table sort buttons were already fixed/proven by `.soup-table-th__sort-btn` hit-extension coverage | `console/src/styles/primitives.css`; `tests/browser/target-size.test.tsx` |
| DD-20 | Framer Motion transitions hard-coded (no reduced-motion path) | app-root MotionConfig reducedMotion="user" wrapper (App.tsx) landed with the B2-era closures; JS springs now honor the user preference | App.tsx MotionConfig on feat/soup-v3-foundation; register row was stale-open until the 2026-06-12 residual audit |
| DD-25 | Toast motion literals bypass motion tokens | Toast motion moved to shared JS token mirrors (`motionDurations`, `motionEasings`, `toastMotion`); `ToastProvider` consumes those objects by reference and wraps the portal stack in `MotionConfig reducedMotion="user"` because the provider sits outside App's MotionConfig | `console/src/lib/motion.ts`; `console/src/hooks/use-toast.tsx`; `tests/console/log-theme-motion.test.ts`; `tests/console/use-toast.test.tsx` |
| DD-17 | Inbox chat list focus-visible ring missing | ChatListItem rows now keep the `c-chat-item` focus-ring class; `.c-chat-item:focus-visible` defines the tokenized inset ring; trusted browser coverage proves `:focus-visible` and non-`none` box-shadow on the focused option | `tests/browser/a11y-contracts.test.tsx`; `tests/console/chat-list-item.test.tsx`; `tests/console/design-token-classes.test.ts`; adoption-audit M2 |
| DD-15 | Segmented controls — residual MetricsTab Tokens/Sessions panel switcher | MetricsTab now uses the Tabs primitive for the Tokens/Sessions content switcher, with manual keyboard activation pinned; the Sessions-only data case selects the Sessions panel immediately instead of rendering an empty detail card | `console/src/components/line-detail/MetricsTab.tsx`; `tests/console/metrics-tab.test.tsx`; `tests/console/primitives-tabs.test.tsx` |
| DD-27 | Focus-trap single-Tab escape does not self-heal | `getFocusableElements` now filters candidates to native Tab stops (`tabIndex >= 0`), so roving-tabindex buttons cannot become the forward-Tab boundary; the browser proof now requires repeated trusted Tab presses to stay inside the modal | `console/src/hooks/use-dismissable.ts`; `tests/console/use-dismissable-dark.test.tsx`; `tests/browser/keyboard-proofs.test.tsx` C-B3W3-7 |
| DD-30 | use-exit-presence reopen-mid-dwell leaves reopened modal in closing phase | Reopen cancellation is live: open=true renders force `phase='open'` immediately, the open effect clears stale `closingActive` after the prior close cleanup cancels callbacks, and canceled animationend/fallback paths cannot unmount the reopened shell | `console/src/hooks/use-exit-presence.ts`; `tests/console/use-exit-presence.test.tsx` duration-stub regression; `console/eslint-waivers.yaml` WVR-015 re-pinned to the reset write |
| DD-19 | Modal exit motion instant; background inert not implemented | B5: closing-phase exit keyframes with guarded animationend + computed-duration fallback (use-exit-presence), refcounted app-root inert with the toast live region portaled to body and inert release before focus restore; drawer holds the documented non-modal exception | impl commit on feat/soup-v3-foundation; jsdom pins in primitives-modal tests; animated path proven by the browser-motion suite 3/3; cited in d7-evidence.md |
| DD-21r | Wizard/modal tablists incomplete keyboard contracts | GroupDetailModal at wave 3 (21-test suite incl. tablist roving), ConfigStep + ModelAuthStep wizard tablists on Tabs with 13 keyboard-contract pins; every named leg coded and tested; the MetricsTab content-panel pair rides the DD-15 ruling to the C3 LineDetail pass | wave-3 migration commit + the wizard-tablists commit on feat/soup-v3-foundation; keyboard contracts pinned per site |
| DD-6 | Status shape law not rendered | C2.1 `fb67b43f` | c2-1-evidence.md |
| DD-7 | No modal focus-restoration primitive | C2.2 `59ceeb4f` | c2-2-evidence.md |
| DD-4 | Geist via Google Fonts runtime import | self-hosted woff2 (vercel/geist-font v1.7.2, SIL OFL 1.1; sha256 provenance in console/public/fonts/README.md); CDN import removed | fonts.css + public/fonts on feat/soup-v3-foundation |
| DD-18 (Fleet legs) | Fleet KPI/chart/table stacking + squeeze, chart-expand layout animation, log wrap policy, fixed side-panel law for Fleet (drawer squeeze 900px container), Fleet pane stacking below lg + short-window page scroll | C2.3 `1f9bde39` + `682558f7` + stacking fix | c2-3-evidence.md (live QA rounds 1–3, both themes, 390/768/1024/1280/1440 + 1440×500) |
| DD-21 (Fleet table half) | Fleet table mouse-first th/tr onClick, no keyboard | C2.3 `1f9bde39` (TableHeaderCell sort buttons + aria-sort; row Enter activation; tested) | c2-3-evidence.md |
| DD-11 | SummaryTab connection-health map outside status-map | B1 `048a26f7` (CONNECTION_MAP/resolveConnection, fail-visible, tested) | b1-evidence.md |
| DD-21r (LineDetail leg) | LineDetail hand-rolled tablist, no keyboard | B1 `d7feb2a4`+`3f4e4d0b` (Tabs primitive + adoption, live-verified) | b1-evidence.md |
| DD-18r (LineDetail leg) | LineDetail header/tabs overflow | B1 `92deece2`+`883a13b4` (truncation, wrap, identity floor, governed tab x-scroll — live-measured) | b1-evidence.md |
| DD-12 | LinePicker combobox semantics | B2 `8ca44a4f` (Popover rebuild, aria-activedescendant, keyboard contract jsdom-pinned; trusted-event confirm = D7) | b2-evidence.md |
| DD-13 | TagInput removable-Pill migration, labeled removes | B2 `a13cdde3` | b2-evidence.md |
| DD-14 | CardSelector radiogroup semantics | B2 `75f87e3a` | b2-evidence.md |
| DD-16 | Search-picker family unified on Popover; orphan ContactSearch deleted | B2 `2b0a9591` (caller greps recorded) | b2-evidence.md |
| DD-31 | UnlockScreen off-system and partially broken | 2026-06-14 UnlockScreen restyle slice: wrapper moved to semantic surface/border/spacing tokens; password field uses `TextInput` without caller style overrides; submit uses `Button` primary; alert uses `c-error`; focused test pins primitive-owned styling; shadow ratchet lowered `UnlockScreen` legacy-token findings 3→0; Playwright production-lock proof passed dark+light at 390×844 with no overflow | `UnlockScreen.tsx`; `tests/console/unlock-screen.test.tsx`; `lint-shadow-baseline.json`; `design-burndown-baseline.json` |
| DD-41 | Component-local viewport JS owner missing | Introduced the sanctioned `useViewportPlacement` owner helper, migrated `MessageBubble` detail-card placement off its direct `window.innerWidth` read, and promoted `soup/no-raw-viewport-js` to package-script fail-on-rule with the real-source inventory pinned to zero | `console/src/hooks/useViewportPlacement.ts`; `console/src/components/MessageBubble.tsx`; `tests/console/use-viewport-placement.test.ts`; `tests/scripts/design-resilience-audit.test.ts`; `console/package.json` |
| DD-40 | Modal max-height used static viewport units | `--modal-max-h` now uses `85dvh`, modal.md/tokens-v3 cite the dynamic viewport law, and a token guard rejects plain `vh` on height-style component tokens | `console/src/styles/tokens.component.css`; `docs/design-system/03-spec/components/modal.md`; `docs/design-system/03-spec/tokens-v3.md`; `tests/console/design-token-classes.test.ts` |
| DD-45 | Viewport-height units should be `dvh`, not `vh` | Migrated the two remaining outliers (`8vh` modal padding clamp -> `8dvh`, `UnlockScreen` `min-h-screen` -> `min-h-dvh`) and promoted `soup/no-static-viewport-height` in `design:resilience`; real-source inventory is pinned to zero, with a root-suite companion inventory test | `console/src/styles/primitives.css`; `console/src/components/UnlockScreen.tsx`; `console/scripts/check-design-resilience.mjs`; `tests/scripts/design-resilience-audit.test.ts`; `tests/console/viewport-height-unit-inventory.test.ts`; `console/package.json` |
| DD-42 | Raw-CSS focus suppression unguarded | Promoted `soup/no-raw-css-focus-suppression` through `design:resilience`: raw `.css` `outline:none`/`outline:0` now fails unless every suppressed selector has a tokenized `:focus-visible` outline + offset. `.fc` gained a forced-colors-safe outline, `.c-chat-item:focus-visible` stopped suppressing outline and now keeps both outline and its tokenized inset shadow, and real-source findings are pinned to zero. | `console/scripts/check-design-resilience.mjs`; `console/src/styles/composites.css`; `tests/scripts/design-resilience-audit.test.ts`; `tests/console/css-focus-suppression-inventory.test.ts`; `tests/console/design-token-classes.test.ts`; `tests/browser/a11y-contracts.test.tsx`; `console/package.json` |
