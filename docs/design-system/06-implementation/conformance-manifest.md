# Design-Conformance Manifest — SOUP v3 (Stage E closeout deliverable, pre-staged)

Living table mapping every G2-locked design decision to its spec source, code path,
token/primitive carrier, test/check, evidence, and CURRENT result. Stage E completes this
table; until then rows are honestly marked. Result vocabulary:

- **PASS** — decision implemented and proven by the cited evidence on the current tree.
- **PENDING (stage)** — proof does not exist yet; the named stage owns it.
- **INCONCLUSIVE** — partially proven; the cell names exactly what is missing.

Path conventions: spec sources are relative to `docs/design-system/`; code and test paths
are relative to the repository root (implementation branch `feat/soup-v3-foundation`).
Evidence packets live in `docs/design-system/06-implementation/`. Debt IDs (DD-*) resolve
in `06-implementation/design-debt-register.md`; lint waivers (WVR-*) in
`console/eslint-waivers.yaml`.

| Decision | Spec source | Code path | Token/primitive | Test/check | Evidence | Result |
|---|---|---|---|---|---|---|
| SOUP UI-facing brand scope — console only; protected WhatSoup boundary (services, protocol, CLI, repo stay WhatSoup) | `03-spec/brand.md` §4; `05-cutover/branding-touchpoints.md` | `console/src/**` + `console/index.html` + `docs/console-guide.md` (Nav nameplate + UpdateModal title + index.html `<title>SOUP Console</title>` + guide vocabulary flipped; protected surfaces untouched) | vocabulary table `brand.md` §2 | `console/scripts/design-regression.sh` protected-contract checks (all present) + check 8 title pin re-pinned to `SOUP Console`; brand-regression lint (0 hits — split wordmark + UpdateModal ternary both cleared); "Soup Kitchen" ×0 counter (console-guide §Pages/§metrics refs flipped to Fleet; Ops empty-state + nav/shortcut labels prior); peripheral-brand-regression suite pins the title exactly | d1-3-4-evidence.md; C4 straggler PR 2026-07-19 | LANDED (C4) — boundary holds (protected contracts verified); user-visible vocabulary flip complete: nameplate, UpdateModal, Operator copy, document-shell title, console-guide refs |
| Geist typography (sans + mono pair, closed named ramp) | `03-spec/typography.md` §1–2 | `console/src/styles/fonts.css` (@font-face, weights 400/500/600); `console/public/fonts/` (six woff2 + provenance README: vercel/geist-font v1.7.2, SIL OFL 1.1, per-file sha256); `console/src/styles/tokens.primitive.css` (font stacks + DD-26 fallback bridge) | `--font-sans` / `--font-mono` + named type ramp | build + render review both themes; `rg -i 'googleapis|gstatic' console/src console/index.html` = 0 hits (verified for this manifest); sha256 table in the fonts README; `tests/console/design-token-type-ramp.test.ts` pins the 9-token fallback bridge (incl. `--type-nameplate`, promoted from pending when the SOUP nameplate landed) and 3 pending spec tokens | d1-2-evidence.md (faces + ramp); commit `7e897756` (self-host, CDN import removed); register DD-4 CLOSED (`7da1cd90`) | INCONCLUSIVE/PARTIAL — self-hosted Geist faces pass and bridge rendering is deterministic; DD-26/DD-37 remain open for the final 12-token spec ramp and 12px floor alignment |
| SOUP nameplate (engraved instrument label: mono spaced-caps SOUP, optical-centering rules, no smaller variant) | `03-spec/brand.md` §1; locked in `02-directions/iterations/v2.html` nameplate lockup | `console/src/components/Nav.tsx` renders the `.soup-nameplate` lockup (tick + `SOUP` wordmark); styled in `console/src/styles/primitives.css` (`.soup-nameplate*`) | `--type-nameplate` (reserved; defined in `tokens.primitive.css`, consumed only by `.soup-nameplate__wm`) | brand-regression lint split-sibling detector now CLEAN (0 hits); `tests/console/nav-status.test.tsx` pins the nameplate render; `nameplate-reserved` rule stubbed | d1-3-4-evidence.md baseline | LANDED (C4) |
| Teal tick — v2.html locked: one 8×8px square, radius 1px, fill mode-passive teal ("brand heritage, not the action accent"), 12px gap before the wordmark, never animates | `02-directions/iterations/v2.html` `.nameplate .tick` + nameplate decision row; codified `03-spec/brand.md` §1.1 rule 3 | `console/src/styles/primitives.css` `.soup-nameplate__tick` (8×8px, radius 1px, `--sp-3` gap) | `--mode-passive-solid` (NOT `--accent`) | nameplate render test (`nav-status.test.tsx`) + live review | d1-3-4-evidence.md baseline | LANDED (C4) |
| Dark theme | `03-spec/tokens-v3.md` §3; `03-spec/color.md` §3–4 | `console/src/styles/tokens.semantic.css` dark scope; `console/src/hooks/use-theme.ts`; pre-paint init in `console/index.html` | semantic token set on `data-theme` (html), base `#0E1013` | `tests/console/use-theme.test.tsx` (6 tests); `console/scripts/check-theme-parity.mjs` (123 tokens both scopes); live QA all migrated surfaces | d1-2-evidence.md; c2-3-evidence.md QA matrix | PASS |
| Light theme (designed, not inverted: hairlines + real shadows + darker saturated chromatics; AA fixes) | `03-spec/color.md` §3–4 + §5 MUST-FIX register | `console/src/styles/tokens.semantic.css` light scope; `color-scheme: light` per scope | per-theme literals incl. AA fixes (`--text-3` #7C8490, ok #15722F, warn #855900, passive #096853) | parity script; computed-style contrast probes; live QA light columns | d1-2-evidence.md; c2-2-evidence.md (color-scheme); c2-3-evidence.md QA matrix | PASS |
| Elevation / surface ladder (4-step surface + 3-step ink + 3-step edge ladders) | `03-spec/tokens-v3.md` §3.1–3.3 | `console/src/styles/tokens.semantic.css` | `--surface-*` / `--text-1/2/3` / `--border-*` | token-resolution diff (D1 cutover); computed-style probes; design-regression legacy-token burn-down counters | d1-1-evidence.md; d1-2-evidence.md | PASS |
| Electric-blue accent as the single action accent (primary buttons, links, focus, selection) | `03-spec/color.md` §2 (six channels + one accent) | `console/src/components/primitives/Button.tsx`; `console/src/styles/primitives.css` | `--accent` / `--accent-fg` (primary re-pointed from the old ok-green, G2-sanctioned) | `tests/console/primitives-button.test.tsx`; AA tables `color.md` §4 (accent rows pass); live review (electric-blue "Add Line") | c2-1-evidence.md | PASS |
| Semantic color system — 3 tiers (primitive / semantic / component) with must-not-own boundaries | `03-spec/tokens-v3.md` §1 | `console/src/styles/tokens.primitive.css`, `tokens.semantic.css`, `tokens.component.css` | the tier file split itself (41 semantic vocabulary tokens at cutover) | design-token tests (3 files); build + utility probes; zero-visual-change mechanical proof at the split | d1-1-evidence.md | PASS |
| Shape-coded status law + text-label pairing (disc/diamond/square/outline 8px, label always; fail-visible unknown) | `03-spec/components/badge.md` | `console/src/lib/status-map.ts` (single rendering driver); `console/src/components/primitives/Badge.tsx` | `--status-shape-size`; status/mode/connection maps | `tests/console/badge-components.test.tsx` (exact contract values); unknown-status fail-visible tested; live review both themes | c2-1-evidence.md (DD-6 closed); c2-3-evidence.md (status cells) | PASS |
| Table densities — default 36px / compressed 28px, two designed densities only | `03-spec/layout-density.md` §3 | `console/src/components/primitives/Table.tsx` (`data-density` attr) | `--row-default` 36 / `--row-compressed` 28 | `tests/console/primitives-table.test.tsx`; live QA 28px compressed rows both themes | c2-3-evidence.md | PASS |
| Toolbar anatomy (fixed zone order) | `03-spec/components/toolbar.md` | `console/src/components/primitives/Toolbar.tsx` | `soup-toolbar*` band in `console/src/styles/primitives.css` | `tests/console/primitives-toolbar.test.tsx`; `tests/browser/keyboard-proofs.test.tsx` (trusted Chromium roving proof); live QA (anatomy + counts, graceful wrap at 768); roving tabindex tests cover ArrowLeft/Right/Home/End, Tab exit, search-input text editing, and focus-entry tab-stop sync | c2-3-evidence.md | PASS — roving arrow-key support now implemented; no remaining D7 no-roving deviation |
| Modal/dialog law — Escape=cancel (always available, safe action), single dismissal owner, focus trap + restoration, one-level stacking | `03-spec/components/modal.md` | `console/src/components/primitives/Modal.tsx`; `console/src/hooks/use-dismissable.ts`; `console/src/hooks/use-exit-presence.ts`; `console/src/hooks/use-background-inert.ts` | `--scrim`; modal size tokens 480/560/720 | `tests/console/primitives-modal.test.tsx`; `tests/console/use-exit-presence.test.tsx`; `tests/console/use-background-inert.test.tsx`; stacked-Escape single-fire tested; `aria-labelledby` verified live | c2-2-evidence.md (DD-7 closed); b5-evidence.md / d7-evidence.md (DD-19 closed) | PASS |
| Drawer/inspector law — squeeze as flex sibling (no scrim) at ≥900px container, overlay+scrim below; focus retarget law | `03-spec/components/drawer.md` incl. the C2.3 amendment block (1080→900px container; rationale recorded) | `console/src/components/primitives/Drawer.tsx`; `restoreFocus` override on use-dismissable | drawer width `min(360px, 86%)`; `soup-drawer-scrim` | `tests/console/primitives-drawer.test.tsx`; live-measured mode flip; close-after-retarget focus tests | c2-3-evidence.md | PASS |
| Log-stream pattern — 4-lane grid, bordered letter tags (E/W/I/D 18×16 chips) | `03-spec/components/log-stream.md` | `console/src/components/primitives/LogStream.tsx`; adopted by LogsTab + Ops | `soup-log*` band; legacy lane vars deleted (zero refs pinned) | `tests/console/primitives-log-stream.test.tsx`; regression check 16; filtered-empty + error/Retry tested; live QA both themes | c2-3-evidence.md | PASS — DD-22 open separately (virtualization awaits a streaming source), non-blocking |
| Tabs law — roving tabindex, arrow/Home/End with wrap, manual activation, accent underline | `03-spec/components/tabs.md` | `console/src/components/primitives/Tabs.tsx` (`d7feb2a4`); LineDetail adoption (`3f4e4d0b`); GroupDetailModal tab strip via Tabs inside Modal (`d73bef54`, B3 wave 3) | `soup-tabs*` band | `tests/console/primitives-tabs.test.tsx`; `tests/console/line-detail-tabs.test.tsx`; `tests/console/group-detail-modal.test.tsx` (31 tests incl. tablist roving and Escape stack) | b1-evidence.md (accepted, verdict PASS — conformance row pins the accent underline against the mode-colored anti-pattern) | PASS |
| Reduced-motion off-and-instant (removed, not shortened) | `03-spec/motion.md` §9 | `console/src/styles/composites.css`; `console/src/styles/primitives.css`; `console/src/App.tsx`; `console/src/hooks/use-toast.tsx` | media-query policy + MotionConfig `reducedMotion="user"` at the app root and toast portal | `tests/console/reduced-motion-config.test.tsx`; `tests/console/log-theme-motion.test.ts`; `tests/console/use-toast.test.tsx`; `tests/browser/b5-inert-toast.test.tsx` true `open→false` reduced-motion close proof | d1-3-4-evidence.md; b5-evidence.md; current focused jsdom + browser reduced-motion runs | PASS — MotionConfig and tokenized toast motion are current; Chromium reduced-motion context now proves modal close through `open={false}` removes the shell with no closing dwell, and toast portal liveness includes dismiss clickability outside the inert subtree |
| ok-breathing as the ONLY ambient loop (2400ms; any new loop needs a G-gate decision) | `03-spec/motion.md` §8 | breathe keyframes in `console/src/styles/composites.css` | ambient budget = one loop; typing ellipsis classified loading-choreography | `no-second-ambient-loop` lint rule (stubbed); WVR-005/006 waive shimmer + typing-bounce | d1-3-4-evidence.md | INCONCLUSIVE — budget law specced and waiver-tracked; waivered loops await C5 motion disposition and rule activation |
| crit-blink rejected (G2) — no critical-status blinking anywhere | `03-spec/motion.md` §8 | none (absence is the deliverable) | n/a | `rg -i blink console/src` = 0 hits (verified for this manifest); conformance review checkpoint | c2-1-evidence.md (conformance row) | PASS |
| Glass limited to header chrome + scrim — never under dense data | `02-directions/iterations/v2.html` glass-chrome note; `03-spec/tokens-v3.md` §3 (chrome glass tokens) | scrim live in `console/src/styles/primitives.css` (modal backdrop, drawer overlay); `--chrome-glass` defined both themes, no consumer yet | `--scrim`; `--chrome-glass` | grep: no backdrop-filter anywhere in `console/src` (constraint holds trivially); scrim behavior tested with Modal/Drawer | c2-2-evidence.md; c2-3-evidence.md | INCONCLUSIVE — scrim leg proven; header-chrome glass treatment lands with the C3 nav slice |
| 24px minimum target floor for every interactive element (WCAG 2.2) | `03-spec/layout-density.md` §4 | primitives carry min sizes / padded hit areas (xs Button 24px; sm Pill now computes to a real 24px target; removable-X and table sort buttons use proven hit-extension bands) | `min-width/min-height: 24px` contracts plus pseudo-element hit-extension proofs where visual density stays below 24px | jsdom class contracts + `tests/browser/target-size.test.tsx` — 40 computed-box / hit-test assertions in real Chromium, including sm Pill computed-box closure, removable-X top+bottom reach, and table sort-button §8 closure assertions | c2-1-evidence.md (DD-10 started); c2-2-evidence.md (sm pill origin); d7-evidence.md (battery origin); current `target-size.test.tsx` run | PASS — DD-10 closed: sm interactive pills no longer depend on the Chromium-blind bottom pseudo-element path, removable-X bottom reach is hit-tested, and table sort buttons remain fixed/proven |
| 80-character measure for running text | `03-spec/typography.md` §5; `03-spec/layout-density.md` §6 (prose primitive) | no consumer — `rg 80ch console/src` = 0 (verified for this manifest) | prose composition primitive (unbuilt) | none yet | none yet | PENDING (C3 per-screen polish) |
| Responsive layout rules (reflow posture, stacking, governed overflow valves) | `03-spec/layout-density.md` §5 | Fleet legs closed: KPI wrap, chart stacking, table squeeze, pane stacking, short-height scroll (`console/src/pages/` Fleet path); LineDetail header/tabs leg closed at B1; Inbox contact-pane collapse landed at B4 (`68a7beda`) | container-driven squeeze + governed x-scroll idiom | live QA matrix at 390/768/1024/1280/1440 + 1440×500, both themes; + deterministic browser viewport matrix `tests/browser/viewport-matrix.test.tsx` (`d80e9726` — Fleet stacking flip probed at 1023/1024, LineDetail h1 truncation + 9-tab x-scroll at 390, overflow sweeps; `4fbad4ef` — Inbox contact-pane rows and 900px drawer flip proof) | c2-3-evidence.md (DD-18 Fleet legs closed); b1-evidence.md (LineDetail leg); d7-evidence.md; b4-evidence.md | INCONCLUSIVE — DD-18r remainder open only for nav width pressure beyond label hiding and side-panel law for non-Fleet surfaces; Inbox viewport rows, deterministic drawer flip, legacy modal sizing SSOT, and MessageBubble positioning are no longer open implementation legs |
| Token/primitive enforcement (lint + regression tripwires) | `04-enforcement/lint-plan.md`; per-spec enforcement-hooks sections | `console/eslint-rules/index.mjs` (soup/* plugin); `console/eslint.config.shadow.mjs`; `console/scripts/design-regression.sh`; `console/scripts/check-shadow-baseline.mjs`; design source audits in `console/scripts/check-*.mjs` | ratchet baseline `console/lint-shadow-baseline.json` — 1, rule×file buckets | `verify:console-design`: theme parity 123; shadow baseline 1/1; design-regression 20 checks with blocking set `1 2 6 8 10 12 13 14 15 16 17 19` PASS; color/resilience fail-on-rule lanes PASS, including `soup/no-raw-css-focus-suppression`, `soup/no-raw-viewport-js`, and `soup/no-static-viewport-height` at zero; brand-assets favicon-link rule PASS; burndown 56 total / 0 blocking (brand-regression ratcheted 2→0 when the SOUP nameplate landed; half-step ratcheted 63→59 when the top bar became the left rail then 59→54 when the dead `.c-btn` recipe was deleted, then rose 54→55 when CheckpointsTab reused the established `--sp-0h` status-badge idiom byte-identical to its sibling tabs (retirement of all `--sp-0h` sites tracked under DD-9); raw-dimension-css ratcheted 18→12 when 1px hairlines and visually-hidden recipes adopted `--bw`, then 12→11 with the `.c-btn` deletion, then 11→0 when off-grid component geometry adopted `tokens.component.css` tokens — leaving only the at-ceiling non-blocking half-step 55 and utility-smell 1); `test:design-guards` 14 files / 182 tests | adoption-audit.md (live shadow-baseline rollup); qa-hardening.md; current guard output | INCONCLUSIVE — promoted design gates are blocking in the shared push/release/tag design chain, but residual shadow debt remains in the 1-warning ratchet and the brand visual/PWA asset findings stay report-only until the approved identity asset set lands |

## Current tally

24 rows: **16 PASS · 2 LANDED · 1 PARTIAL · 4 INCONCLUSIVE · 1 PENDING** (2026-06-16 update:
the SOUP nameplate + teal-tick rows flip PENDING → LANDED and the brand-scope row flips
INCONCLUSIVE → PARTIAL — Nav renders the `.soup-nameplate` lockup and UpdateModal flips to
"Update SOUP"; brand-regression lint ratchets 2 → 0 and burndown blocking drops 20 → 18.
Earlier, as of the 2026-06-12 D7 evidence refresh:
the computed-box/viewport batteries landed and were reproduced fresh (d7-evidence.md, 76/76 +
3/3) but flipped NO rows. 2026-06-14 update: the target-size suite now passes 40 assertions;
sm interactive pills have a real 24px computed target, removable-X top/bottom reach is proven,
and the table sort-button floor leg remains fixed/proven, so the target-size row flips PASS.
2026-06-14 update: the reduced-motion browser proof now closes a modal through
`open={false}` in Chromium's `prefers-reduced-motion: reduce` context and proves no
closing dwell remains, so the reduced-motion row flips PASS.
Earlier refresh: Tabs
flipped on b1-evidence.md acceptance plus the B3 wave-3 Tabs-in-Modal suite; typography flipped
on the DD-4 self-host closure. Next expected flips wait on the remaining DD-18r legs and the
B4/D6 acceptance packets — not pre-credited here).

## Completion rule

Stage E may not render its verdict while any row reads PENDING or INCONCLUSIVE. Each such
row must either (a) flip to PASS with evidence cited from this repository on the
then-current tree — fresh evidence, not a stale packet reference — or (b) carry an explicit
accepted-debt annotation pointing at a design-debt-register entry whose "Blocks final
acceptance?" cell says no and whose expiration condition is still live. Rows backed by a
register entry marked as blocking (currently DD-8, DD-18r, DD-35, and any future peers)
cannot take the accepted-debt exit; they must be closed. Overclaiming a row is itself a
Stage E defect: any PASS found without reproducible evidence reverts the row and reopens
the gate.
