# draft(cluster): portability + test-coverage fixes (3 P3-P5 issues)

Closes #2321, #2728, #2363

Docker audit: 1 HIGH (macOS home transcoding) in src/runtimes (#2321, P5). scripts/ci-control-manifest.ts shipped without its dedicated tests (#2728, P3). Remove fixed-time child PID race from new-command ownership coverage (#2363, P3).

All guards pass.
