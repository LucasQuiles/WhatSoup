# Valid change record fixture: a bugfix declared low-risk. The low declaredRisk MUST NOT
# excuse the bugfix's Regression-For obligation (that obligation is enforced at the trailer
# level); this fixture is paired with a Regression-For trailer in the passing bugfix case.
id: CR-valid-bugfix
intent: bugfix
declaredRisk: low
summary: Fix the trailer parser off-by-one.
components:
  - scripts/ci-change-record
regression: true
