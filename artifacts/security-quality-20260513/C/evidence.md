# Track C Evidence

- Base SHA: 91ea6c5d0394e681641a4888eb4d01c75fdc021e
- Branch: fix/knowledge-tool-env-log-rename-20260513-codex
- Changed files:
  - src/mcp/tools/knowledge.ts
  - artifacts/security-quality-20260513/C/evidence.md
- Commands run:
  - `git fetch origin main`
  - `git worktree add -b fix/knowledge-tool-env-log-rename-20260513-codex <track-c-worktree> origin/main`
  - `npm run typecheck`
  - `npm ci`
  - `npm run typecheck`
  - `rg -n "log\\.(warn|info|error|debug).*apiKeyEnv|apiKeyEnv.*log\\.(warn|info|error|debug)" src/mcp/tools/knowledge.ts && echo "BLOCKED - apiKeyEnv still appears on a log call" || echo "OK"`
  - `rg -n "process\\.env\\[envVarName\\]|envVarName" src/mcp/tools/knowledge.ts`
  - Post-#595 rebase onto `origin/main` `91ea6c5d0394e681641a4888eb4d01c75fdc021e`
  - Re-ran `npm run typecheck` and both privacy greps
  - GitHub CodeQL check on PR #598 reported one high alert at `src/mcp/tools/knowledge.ts:188`; the annotation said the logged value still derived from `apiKeyEnv`.
  - Follow-up local patch removed the dynamic env-var name from the missing-key warning payload entirely.
  - `rg -n "log\\.(warn|info|error|debug).*apiKeyEnv|apiKeyEnv.*log\\.(warn|info|error|debug)|log\\.(warn|info|error|debug).*envVarName|envVarName.*log\\.(warn|info|error|debug)" src/mcp/tools/knowledge.ts && echo 'BLOCKED — sensitive env name still reaches a log call' || echo 'OK — no apiKeyEnv/envVarName log sink'`
  - `git diff --check`
  - `npm run typecheck`
  - `npm test -- tests/mcp/tools/knowledge.test.ts --pool=forks`
- Results:
  - Branch rebased cleanly onto the post-Track-D main SHA.
  - Initial `npm run typecheck` failed because `tsc` was not installed in the fresh worktree.
  - `npm ci` completed successfully from the committed lockfile.
  - Rerun `npm run typecheck` passed.
  - Privacy check returned `OK`; `apiKeyEnv` does not appear on a log call in `src/mcp/tools/knowledge.ts`.
  - `envVarName` check found expected local assignment, `process.env[envVarName]`, and `log.warn({ envVarName }, ...)`.
  - Post-rebase typecheck and privacy checks passed with the same expected `envVarName` hits.
  - PR #598 CodeQL check failed before the follow-up patch because `envVarName` was still a logged value derived from `memoryConfig.apiKeyEnv`.
  - Follow-up privacy check returned `OK — no apiKeyEnv/envVarName log sink`.
  - `git diff --check` passed.
  - Follow-up `npm run typecheck` passed.
  - Targeted knowledge-tool tests passed: 1 file, 19 tests.
- Masked/inconclusive failures:
  - None. The first typecheck failure was an environment setup miss in the fresh worktree and passed after dependency install.
- External actions pending approval:
  - Follow-up commit is local only. Pushing the new commit to PR #598 requires an explicit Track C push approval.
  - Merge remains gated by G-C2 after CI and CodeQL are green.
- Residual risk:
  - CodeQL alert #5 requires a new PR scan after the follow-up commit. No dismissal attempted.
