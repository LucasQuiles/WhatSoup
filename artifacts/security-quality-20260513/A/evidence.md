# Track A Evidence

- Base SHA: 91ea6c5d0394e681641a4888eb4d01c75fdc021e
- Branch: fix/workflow-least-privilege-20260513-codex
- Changed files:
  - .github/workflows/quality.yml
  - .github/workflows/whatsoup-guard.yml
  - artifacts/security-quality-20260513/A/evidence.md
- Commands run:
  - `git fetch origin main`
  - `git rev-parse origin/main`
  - `git worktree add -b fix/workflow-least-privilege-20260513-codex <track-a-worktree> origin/main`
  - `rg -n "GITHUB_TOKEN|gh |gh\\s+api|pull-request|issue|release|package|pages|id-token|attestation" .github/workflows/quality.yml .github/workflows/whatsoup-guard.yml | tee artifacts/security-quality-20260513/A/workflow-write-surface.txt || true`
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "OK #{f}" }' .github/workflows/quality.yml .github/workflows/whatsoup-guard.yml`
  - `git diff --check`
  - Post-#595 rebase onto `origin/main` `91ea6c5d0394e681641a4888eb4d01c75fdc021e`
  - Re-ran YAML parse, write-surface scan, and `git diff --check`
- Results:
  - Branch rebased cleanly onto the post-Track-D main SHA.
  - Added top-level `permissions: contents: read` to both workflows.
  - Workflow write-surface scan found one benign match in an existing comment: `.github/workflows/quality.yml:26` mentions `package.json`; no `GITHUB_TOKEN`, `gh`, pull request, issue, release, package publishing, pages, id-token, or attestation write surface was found.
  - YAML parse check passed for both workflows.
  - `git diff --check` passed with no whitespace errors.
- Masked/inconclusive failures: None.
- External actions pending approval: Push, PR creation, merge, and post-merge CodeQL alert confirmation were not performed.
- Residual risk: CodeQL alerts #1 and #2 remain unconfirmed locally until this branch is pushed, merged, and GitHub code scanning re-runs.
