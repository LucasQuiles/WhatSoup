name: Quality

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['24.x', '25.x']
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Local dev uses .nvmrc (single version for fast iteration);
      # CI runs a matrix to validate the engines range declared in package.json.
      - name: Setup Node ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Python test dependencies
        run: python3 -m pip install --user pytest

      - name: Typecheck
        run: npm run typecheck

      - name: Typecheck scripts/
        run: npm run typecheck:scripts

      - name: Typecheck (all)
        run: npm run typecheck:all

      - name: Import boundary guard
        run: npm run guard:boundaries

      - name: Repo hygiene staged smoke
        run: npm run guard:repo:staged

      - name: Repo hygiene commit-author guard
        run: |
          if [ -n "${GITHUB_BASE_REF:-}" ]; then
            git fetch origin "$GITHUB_BASE_REF:refs/remotes/origin/$GITHUB_BASE_REF"
          fi
          npm run guard:repo:commit-authors

      - name: Install test-integrity plugin
        env:
          TEST_INTEGRITY_DEPLOY_KEY: ${{ secrets.TEST_INTEGRITY_DEPLOY_KEY }}
        run: |
          if [ -z "$TEST_INTEGRITY_DEPLOY_KEY" ]; then
            echo "::error::TEST_INTEGRITY_DEPLOY_KEY secret is required to install the private LucasQuiles/test-integrity plugin"
            exit 2
          fi
          mkdir -p "$HOME/.claude/plugins" "$HOME/.ssh"
          chmod 700 "$HOME/.ssh"
          printf '%s\n' "$TEST_INTEGRITY_DEPLOY_KEY" > "$HOME/.ssh/test_integrity_deploy"
          chmod 600 "$HOME/.ssh/test_integrity_deploy"
          trap 'rm -f "$HOME/.ssh/test_integrity_deploy"' EXIT
          ssh-keyscan -t ed25519 github.com >> "$HOME/.ssh/known_hosts" 2>/dev/null
          GIT_SSH_COMMAND="ssh -i $HOME/.ssh/test_integrity_deploy -o IdentitiesOnly=yes -o UserKnownHostsFile=$HOME/.ssh/known_hosts" \
            git clone --depth 1 git@github.com:LucasQuiles/test-integrity.git "$HOME/.claude/plugins/test-integrity"

      - name: Test integrity baseline check
        env:
          WHATSOUP_REQUIRE_TEST_INTEGRITY: '1'
        run: npm run guard:test-integrity

      - name: ESLint fitness ring
        run: npm run guard:lint:src

      - name: Tokenomics Python tests
        run: npm run test:tokenomics

      - name: Repo hygiene commit-message smoke
        run: |
          msg="$(mktemp)"
          printf 'chore(ci): guard smoke\n' > "$msg"
          npm run guard:repo:commit-msg -- "$msg"

      - name: Documentation drift guard
        run: npm run guard:doc-drift

      - name: Public surface drift guard
        run: npm run guard:public-surface-drift

      - name: Work index coverage guard
        run: npm run guard:work-index

      - name: Claude settings drift guard
        run: npm run guard:claude-settings

      - name: Publication guard
        run: npm run guard:publication:all

      - name: Node pin consistency guard
        run: npm run guard:node-pin-consistency

      - name: Service unit validity guard
        run: npm run guard:service-units

      - name: Instance config integrity guard
        run: npm run guard:instance-config

      - name: Agent decision polls guard
        run: npm run guard:agent-decision-polls

      - name: Safeguard diagnostics
        run: npm run guard:safeguard-diagnostics

      - name: Repo hygiene tests
        run: npm test -- tests/scripts/repo-hygiene-guard.test.ts --pool=forks

      - name: Secret-leak history scan (advisory)
        continue-on-error: true
        run: npm run guard:repo:scan-history -- 100

      - name: Test suite
        run: npm test -- --pool=forks

      - name: Install console dependencies
        run: npm --prefix console ci

      - name: Console build
        run: npm --prefix console run build
