{
  "_TEMPLATE": "Scaffold for a CAPE live-activation approval. This is NOT a granted approval. To grant: copy to artifacts/live_approval.json (gitignored, environment-specific), set granted_by to the owner identity, set a real reason, and set issued_at_epoch/expires_at_epoch to a positive-duration window in epoch seconds. As shipped, this template FAILS CLOSED if used verbatim: granted_by is empty (-> blocked: missing_grantor) and the window is zero-width (-> blocked: invalid_window). The live_approval_gate decides approved|blocked and performs no live action itself.",
  "schema": "agent-runtime-live-approval",
  "schema_version": "0.1",
  "scope": ["shadow_run"],
  "granted_by": "",
  "reason": "TEMPLATE — replace with the explicit authorization rationale for the named scope",
  "issued_at_epoch": 0,
  "expires_at_epoch": 0,
  "plan_ref": "ROADMAP.md@activation-ladder-shadow"
}
